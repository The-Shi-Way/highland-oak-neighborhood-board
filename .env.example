# Set this to connect to the real AWS backend
# Leave empty to use the built-in mock data
VITE_API_URL=http://localhost:3001

# Production (replace with your actual values after `sam deploy`)
# VITE_API_URL=https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com
